# VIBRAE OS — dependências de runtime (produção)
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
python-multipart>=0.0.9
httpx>=0.27.0
stripe>=7.0.0,<11.0.0
anthropic>=0.49.0
tzdata>=2024.2

# Testes (opcional; não afeta o runtime em produção)
pytest>=8.0.0
pytest-xdist>=3.6.0

# NOTA: 'emergentintegrations' (IA VIBRAE + transcrição) foi removido porque é um
# pacote proprietário do Emergent que não instala em hospedagem pública. Esses recursos
# serão religados na Fase C com os SDKs oficiais (anthropic / openai) e as suas chaves.
